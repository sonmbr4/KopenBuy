<!DOCTYPE html>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Tienda de tecnología con los mejores productos al mejor precio">
    <title>Kopen Buy - Tecnología de Última Generación</title>
    <link rel="stylesheet" href="/CSS/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        .product-card {
            transition: all 0.3s ease;
        }
        .product-card.fade-in-scroll {
            animation: fadeIn 0.6s ease-out forwards;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <%- include('./partials/header.ejs'); %>
        <main class="landing-container">
            <!-- Sección Banner -->
            <section class="product-carousel mb-5">
                <div id="mainCarousel" class="carousel slide" data-bs-ride="carousel">
                    <!--Indicadores-->
                    <div class="carousel-indicators">
                        <button type="button" data-bs-target="#mainCarousel" data-bs-slide-to="0"
                            class="active"></button>
                        <button type="button" data-bs-target="#mainCarousel" data-bs-slide-to="1"></button>
                        <button type="button" data-bs-target="#mainCarousel" data-bs-slide-to="2"></button>
                    </div>

                    <!--Slide-->
                    <div class="carousel-inner rounded-3 overflow-hidden">
                        <!--Oferta 1-->
                        <div class="carousel-item active">
                            <div class="d-block w-100 bg-success bg-opacity-10 p-5" style="height: 400px;">
                                <div class="container h-100">
                                    <div class="row h-100 align-items-center">
                                        <div class="col-md-6">
                                            <span class="badge bg-danger mb-3">OFERTA LIMITADA</span>
                                            <h2 class="display-5 fw-bold mb-3">Control Gamer</h2>
                                            <p class="lead mb-4">hasta 30% de descuento en todos los dispositivos Gamer
                                            </p>
                                            <a href="#" class="btn btn-success btn-lg px-4">Ver oferta</a>
                                        </div>
                                        <div class="col-md-3 d-none d-md-block">
                                            <img src="assets/imagenes/Control-Gamer.png" class="img-fluid" alt="Control"
                                                loading="lazy" onerror="handleImageError(this)">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!--Oferta 2-->
                        <div class="carousel-item ">
                            <div class="d-block w-100 bg-success bg-opacity-10 p-5" style="height: 400px;">
                                <div class="container h-100">
                                    <div class="row h-100 align-items-center">
                                        <div class="col-md-6">
                                            <span class="badge bg-danger mb-3">Nuevos Productos</span>
                                            <h2 class="display-5 fw-bold mb-3">Audifonos</h2>
                                            <p class="lead mb-4">Nuevos Auriculares bluethoot</p>
                                            <a href="#" class="btn btn-success btn-lg px-4">Ver Productos</a>
                                        </div>
                                        <div class="col-md-3 d-none d-md-block">
                                            <img src="assets/imagenes/Auriculares.png" class="img-fluid" alt="Control"
                                                loading="lazy" onerror="handleImageError(this)">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!--oferta 3-->
                        <div class="carousel-item ">
                            <div class="d-block w-100 bg-success bg-opacity-10 p-5" style="height: 400px;">
                                <div class="container h-100">
                                    <div class="row h-100 align-items-center">
                                        <div class="col-md-6">
                                            <span class="badge bg-danger mb-3">OFERTA LIMITADA</span>
                                            <h2 class="display-5 fw-bold mb-3">Portátil Gamer</h2>
                                            <p class="lead mb-4">hasta 30% de descuento en todos los dispositivos Gamer
                                            </p>
                                            <a href="#" class="btn btn-success btn-lg px-4">Ver oferta</a>
                                        </div>
                                        <div class="col-md-3 d-none d-md-block">
                                            <img src="assets/imagenes/Laptop-Gamer-Carousel.png" class="img-fluid"
                                                alt="Control" loading="lazy" onerror="handleImageError(this)">
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!--Controles-->
                        <button class="carousel-control-prev" type="button" data-bs-target="#mainCarousel"
                            data-bs-slide="prev">
                            <span class="carousel-control-prev-icon"></span>
                            <span class="visually-hidden">Anterior</span>
                        </button>
                        <button class="carousel-control-next" type="button" data-bs-target="#mainCarousel"
                            data-bs-slide="next">
                            <span class="carousel-control-next-icon"></span>
                            <span class="visually-hidden">Siguiente</span>
                        </button>
                    </div>
                </div>


            </section>

            <!-- Sección Categorías -->
            <section class="py-5 bg-white" id="categorias">
                <div class="container">
                    <h2 class="text-center mb-5">Categorías</h2>
                    <div class="row g-4">
                        <div class="col-6 col-md-3">
                            <div class="card text-center h-100 border-0">
                                <div class="card-body">
                                    <h5 class="card-title">Portátiles</h5>
                                </div>
                                <a href="#laptops" class="stretched-link"></a>
                            </div>
                        </div>
                        <div class="col-6 col-md-3">
                            <div class="card text-center h-100 border-0">
                                <div class="card-body">
                                    <h5 class="card-title">Teléfonos</h5>
                                </div>
                                <a href="#telefonos" class="stretched-link"></a>
                            </div>
                        </div>
                        <div class="col-6 col-md-3">
                            <div class="card text-center h-100 border-0">
                                <div class="card-body">
                                    <h5 class="card-title">Audio</h5>
                                </div>
                                <a href="#audio" class="stretched-link"></a>
                            </div>
                        </div>
                        <div class="col-6 col-md-3">
                            <div class="card text-center h-100 border-0">
                                <div class="card-body">
                                    <h5 class="card-title">Juegos</h5>
                                </div>
                                <a href="#juegos" class="stretched-link"></a>
                            </div>
                        </div>
                    </div>
                </div>
            </section>


            <!--Seccion Productos Destacados-->
           <!-- Sección Productos Destacados -->
<section class="py-5 product-section">
    <div class="container">
        <h2 class="text-center mb-5 fade-in-scroll">Productos Destacados</h2>
        <div class="row g-4">
            <% if (productosDestacados && productosDestacados.length > 0) { %>
                <% productosDestacados.forEach((producto, index) => { %>
                    <div class="col-md-6 col-lg-3">
                        <div class="card product-card fade-in-scroll">
                            <% if (producto.image) { %>
                                <img src="/uploads/<%= producto.image.includes('/') ? producto.image.split('/').pop() : producto.image %>"
                                     alt="<%= producto.name %>" 
                                     class="card-img-top" 
                                     style="height: 200px; object-fit: cover;"
                                     onerror="this.onerror=null; this.src='/assets/imagenes/placeholder-image.svg'">
                            <% } else { %>
                                <img src="/assets/imagenes/placeholder-image.svg"
                                     class="card-img-top" 
                                     alt="Imagen no disponible"
                                     style="height: 200px; object-fit: contain;"
                                     loading="lazy">
                            <% } %>

                            <div class="card-body">
                                <h5 class="card-title"><%= producto.name %></h5>
                                
                                <% if (producto.category) { %>
                                    <p class="text-primary mb-2">
                                        <small><%= producto.category %></small>
                                    </p>
                                <% } %>

                                <p class="card-text text-muted">
                                    <%= producto.description ? (producto.description.length > 60 ? 
                                        producto.description.substring(0, 60) + '...' : producto.description) : 
                                        'Sin descripción disponible' %>
                                </p>

                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="badge <%= producto.stock <= 0 ? 'bg-danger' : 
                                        (producto.stock < 10 ? 'bg-warning' : 'bg-success') %>">
                                        <%= producto.stock <= 0 ? 'Agotado' : 
                                            (producto.stock < 10 ? 'Casi Agotado' : 'En Stock') %>
                                    </span>
                                    <span class="fw-bold">$<%= producto.price ? 
                                        producto.price.toLocaleString() : '0' %></span>
                                </div>
                            </div>

                            <div class="card-footer bg-white border-0 d-flex flex-column gap-2">
                                <a href="/productos/<%= producto._id %>" 
                                   class="btn btn-outline-primary w-100">Ver Detalles</a>
                                <button class="btn btn-success add-to-cart-btn auth-only" 
                                        style="display: none;" 
                                        data-product-id="<%= producto._id %>">
                                    🛒 Agregar al Carrito
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12 text-center">
                    <p>No hay productos destacados disponibles en este momento.</p>
                </div>
            <% } %>
        </div>
    </div>
</section>


            <!--Seccion Computadoras-->
            <section class="py-5 product-section">
                <div class="container">
                    <h2 class="text-center mb-5 fade-in-scroll" id="laptops">Computadoras</h2>
                    <div class="row g-4">
                        <% if (computerProducts && computerProducts.length> 0) { %>
                            <% computerProducts.forEach((producto, index)=> { %>
                                <div class="col-md-6 col-lg-3">
                                    <div class="card product-card fade-in-scroll">
                                        <% if (producto.image) { %>
                                            <img src="<%= producto.image %>" class="card-img-top product-image"
                                                alt="<%= producto.name %>" loading="lazy" onerror="handleImageError(this)">
                                            <% } else { %>
                                                <img src="/assets/imagenes/placeholder-image.svg"
                                                    class="card-img-top product-image" alt="Imagen no disponible"
                                                    loading="lazy" onerror="handleImageError(this)">
                                                <% } %>
                                                    <div class="card-body">
                                                        <h5 class="card-title">
                                                            <%= producto.name %>
                                                        </h5>
                                                        <% if (producto.category) { %>
                                                            <p class="text-primary mb-2"><small>
                                                                    <%= producto.category %>
                                                                </small></p>
                                                            <% } %>
                                                                <p class="card-text text-muted">
                                                                    <%= producto.description
                                                                        || 'Sin descripción disponible' %>
                                                                </p>
                                                                <div
                                                                    class="d-flex justify-content-between align-items-center">
                                                                    <span
                                                                        class="badge <%= producto.stock <= 0 ? 'bg-danger' : (producto.stock < 10 ? 'bg-warning' : 'bg-success') %>">
                                                                        <%= producto.stock <=0 ? 'Agotado' :
                                                                            (producto.stock < 10 ? 'Casi Agotado'
                                                                            : 'En Stock' ) %>
                                                                    </span>
                                                                    <span class="fw-bold">$<%= producto.price ?
                                                                            producto.price.toLocaleString() : '0' %>
                                                                    </span>
                                                                </div>
                                                    </div>
                                                    <div class="card-footer bg-white border-0 d-flex flex-column gap-2">
                                                        <a href="/productos/<%= producto._id %>" class="btn btn-outline-primary w-100">Ver Detalles</a>
                                                        <!-- Botón de Agregar al Carrito (solo para usuarios logueados) -->
                                                        <button class="btn btn-success add-to-cart-btn auth-only"
                                                            style="display: none;" data-product-id="<%= producto._id %>">
                                                            🛒 Agregar al Carrito
                                                        </button>
                                                    </div>
                                    </div>
                                </div>
                                <% }); %>
                                    <% } else { %>
                                        <div class="col-12 text-center">
                                            <p>No hay computadoras disponibles en este momento.</p>
                                        </div>
                                        <% } %>
                    </div>
                </div>
            </section>


            <!--Seccion Telefonos-->
            <section class="py-5 product-section">
                <div class="container">
                    <h2 class="text-center mb-5 fade-in-scroll" id="telefonos">Teléfonos</h2>
                    <div class="row g-4">
                        <% if (phoneProducts && phoneProducts.length> 0) { %>
                            <% phoneProducts.forEach((producto, index)=> { %>
                                <div class="col-md-6 col-lg-3">
                                    <div class="card product-card fade-in-scroll">
                                        <% if (producto.image) { %>
                                            <img src="<%= producto.image %>" class="card-img-top product-image"
                                                alt="<%= producto.name %>" loading="lazy" onerror="handleImageError(this)">
                                            <% } else { %>
                                                <img src="/assets/imagenes/placeholder-image.svg"
                                                    class="card-img-top product-image" alt="Imagen no disponible"
                                                    loading="lazy" onerror="handleImageError(this)">
                                                <% } %>
                                                    <div class="card-body">
                                                        <h5 class="card-title">
                                                            <%= producto.name %>
                                                        </h5>
                                                        <% if (producto.category) { %>
                                                            <p class="text-primary mb-2"><small>
                                                                    <%= producto.category %>
                                                                </small></p>
                                                            <% } %>
                                                                <p class="card-text text-muted">
                                                                    <%= producto.description
                                                                        || 'Sin descripción disponible' %>
                                                                </p>
                                                                <div
                                                                    class="d-flex justify-content-between align-items-center">
                                                                    <span
                                                                        class="badge <%= producto.stock <= 0 ? 'bg-danger' : (producto.stock < 10 ? 'bg-warning' : 'bg-success') %>">
                                                                        <%= producto.stock <=0 ? 'Agotado' :
                                                                            (producto.stock < 10 ? 'Casi Agotado'
                                                                            : 'En Stock' ) %>
                                                                    </span>
                                                                    <span class="fw-bold">$<%= producto.price ?
                                                                            producto.price.toLocaleString() : '0' %>
                                                                    </span>
                                                                </div>
                                                    </div>
                                                    <div class="card-footer bg-white border-0 d-flex flex-column gap-2">
                                                        <a href="/productos/<%= producto._id %>" class="btn btn-outline-primary w-100">Ver Detalles</a>
                                                        <!-- Botón de Agregar al Carrito (solo para usuarios logueados) -->
                                                        <button class="btn btn-success add-to-cart-btn auth-only"
                                                            style="display: none;" data-product-id="<%= producto._id %>">
                                                            🛒 Agregar al Carrito
                                                        </button>
                                                    </div>
                                    </div>
                                </div>
                                <% }); %>

                                    <% } else { %>
                                        <div class="col-12 text-center">
                                            <p>No hay teléfonos disponibles en este momento.</p>
                                        </div>
                                        <% } %>
                    </div>
                </div>
            </section>

            <!--Seccion Audio-->
            <section class="py-5 product-section">
                <div class="container">
                    <h2 class="text-center mb-5 fade-in-scroll" id="audio">Audio</h2>
                    <div class="row g-4">
                        <% if (audioProducts && audioProducts.length> 0) { %>
                            <% audioProducts.forEach((producto, index)=> { %>
                                <div class="col-md-6 col-lg-3">
                                    <div class="card product-card fade-in-scroll">
                                        <% if (producto.image) { %>
                                            <img src="<%= producto.image %>" class="card-img-top product-image"
                                                alt="<%= producto.name %>" loading="lazy" onerror="handleImageError(this)">
                                            <% } else { %>
                                                <img src="/assets/imagenes/placeholder-image.svg"
                                                    class="card-img-top product-image" alt="Imagen no disponible"
                                                    loading="lazy" onerror="handleImageError(this)">
                                                <% } %>
                                                    <div class="card-body">
                                                        <h5 class="card-title">
                                                            <%= producto.name %>
                                                        </h5>
                                                        <% if (producto.category) { %>
                                                            <p class="text-primary mb-2"><small>
                                                                    <%= producto.category %>
                                                                </small></p>
                                                            <% } %>
                                                                <p class="card-text text-muted">
                                                                    <%= producto.description
                                                                        || 'Sin descripción disponible' %>
                                                                </p>
                                                                <div
                                                                    class="d-flex justify-content-between align-items-center">
                                                                    <span
                                                                        class="badge <%= producto.stock <= 0 ? 'bg-danger' : (producto.stock < 10 ? 'bg-warning' : 'bg-success') %>">
                                                                        <%= producto.stock <=0 ? 'Agotado' :
                                                                            (producto.stock < 10 ? 'Casi Agotado'
                                                                            : 'En Stock' ) %>
                                                                    </span>
                                                                    <span class="fw-bold">$<%= producto.price ?
                                                                            producto.price.toLocaleString() : '0' %>
                                                                    </span>
                                                                </div>
                                                    </div>
                                                    <div class="card-footer bg-white border-0 d-flex flex-column gap-2">
                                                        <a href="/productos/<%= producto._id %>" class="btn btn-outline-primary w-100">Ver Detalles</a>
                                                        <!-- Botón de Agregar al Carrito (solo para usuarios logueados) -->
                                                        <button class="btn btn-success add-to-cart-btn auth-only"
                                                            style="display: none;" data-product-id="<%= producto._id %>">
                                                            🛒 Agregar al Carrito
                                                        </button>
                                                    </div>
                                    </div>
                                </div>
                                <% }); %>

                                    <% } else { %>
                                        <div class="col-12 text-center">
                                            <p>No hay nada relacionado con audio disponibles en este momento.</p>
                                        </div>
                                        <% } %>
                    </div>
                </div>
            </section>

            <!--Seccion Gaming-->

            <section class="py-5 product-section">
                <div class="container">
                    <h2 class="text-center mb-5 fade-in-scroll" id="juegos">Juegos</h2>
                    <div class="row g-4">
                        <% if (gamingProducts && gamingProducts.length> 0) { %>
                            <% gamingProducts.forEach((producto, index)=> { %>
                                <div class="col-md-6 col-lg-3">
                                    <div class="card product-card fade-in-scroll">
                                        <% if (producto.image) { %>
                                            <img src="<%= producto.image %>" class="card-img-top product-image"
                                                alt="<%= producto.name %>" loading="lazy" onerror="handleImageError(this)">
                                            <% } else { %>
                                                <img src="/assets/imagenes/placeholder-image.svg"
                                                    class="card-img-top product-image" alt="Imagen no disponible"
                                                    loading="lazy" onerror="handleImageError(this)">
                                                <% } %>
                                                    <div class="card-body">
                                                        <h5 class="card-title">
                                                            <%= producto.name %>
                                                        </h5>
                                                        <% if (producto.category) { %>
                                                            <p class="text-primary mb-2"><small>
                                                                    <%= producto.category %>
                                                                </small></p>
                                                            <% } %>
                                                                <p class="card-text text-muted">
                                                                    <%= producto.description
                                                                        || 'Sin descripción disponible' %>
                                                                </p>
                                                                <div
                                                                    class="d-flex justify-content-between align-items-center">
                                                                    <span
                                                                        class="badge <%= producto.stock <= 0 ? 'bg-danger' : (producto.stock < 10 ? 'bg-warning' : 'bg-success') %>">
                                                                        <%= producto.stock <=0 ? 'Agotado' :
                                                                            (producto.stock < 10 ? 'Casi Agotado'
                                                                            : 'En Stock' ) %>
                                                                    </span>
                                                                    <span class="fw-bold">$<%= producto.price ?
                                                                            producto.price.toLocaleString() : '0' %>
                                                                    </span>
                                                                </div>
                                                    </div>
                                                    <div class="card-footer bg-white border-0 d-flex flex-column gap-2">
                                                        <a href="/productos/<%= producto._id %>" class="btn btn-outline-primary w-100">Ver Detalles</a>
                                                        <!-- Botón de Agregar al Carrito (solo para usuarios logueados) -->
                                                        <button class="btn btn-success add-to-cart-btn auth-only"
                                                            style="display: none;" data-product-id="<%= producto._id %>">
                                                            🛒 Agregar al Carrito
                                                        </button>
                                                    </div>
                                    </div>
                                </div>
                                <% }); %>

                                    <% } else { %>
                                        <div class="col-12 text-center">
                                            <p>No hay nada relacionado con gaming disponibles en este momento.</p>
                                        </div>
                                        <% } %>
                    </div>
                </div>
            </section>


            <!--Seccion Testimonios-->
            <section class="py-5 bg-light">
                <div class="container">
                    <h2 class="text-center mb-5">Lo que dicen nuestros clientes</h2>
                    <div class="row">
                        <!-- Testimonio 1 -->
                        <div class="col-md-4 mb-4">
                            <div class="card p-4 h-100">
                                <div class="d-flex mb-3">
                                    <i class="bi bi-person"></i>
                                    <div>
                                        <h5 class="mb-0">Ana Pérez</h5>
                                        <div class="text-warning">
                                            ★★★★★
                                        </div>
                                    </div>
                                </div>
                                <p class="mb-0">"¡La mejor laptop que he tenido! Entrega rápida y buen soporte."</p>
                            </div>
                        </div>
                        <!-- Testimonio 2 -->
                        <div class="col-md-4 mb-4">
                            <div class="card p-4 h-100">
                                <div class="d-flex mb-3">
                                    <i class="bi bi-person"></i>
                                    <div>
                                        <h5 class="mb-0">Carlos Gómez</h5>
                                        <div class="text-warning">
                                            ★★★★☆
                                        </div>
                                    </div>
                                </div>
                                <p class="mb-0">"Excelente relación calidad-precio en los smartphones."</p>
                            </div>
                        </div>
                        <!-- Testimonio 3 -->
                        <div class="col-md-4 mb-4">
                            <div class="card p-4 h-100">
                                <div class="d-flex mb-3">
                                    <i class="bi bi-person"></i>
                                    <div>
                                        <h5 class="mb-0">María Rodríguez</h5>
                                        <div class="text-warning">
                                            ★★★★★
                                        </div>
                                    </div>
                                </div>
                                <p class="mb-0">"Los audífonos superaron mis expectativas. ¡100% recomendados!"</p>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Sección Newsletter -->
            <section class="py-5 bg-secondary text-white">
                <div class="container">
                    <div class="row align-items-center">
                        <div class="col-md-6 mb-3 mb-md-0">
                            <h3>Suscríbete a nuestro boletín</h3>
                            <p>Recibe ofertas exclusivas y noticias sobre tus productos favoritos</p>
                        </div>
                        <div class="col-md-6">
                            <form class="input-group">
                                <input type="email" class="form-control" placeholder="Tu email" required>
                                <button class="btn btn-primary" type="submit">Suscribirse</button>
                            </form>
                        </div>
                    </div>
                </div>
            </section>
        </main>

        <%- include('./partials/footer.ejs'); %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
            <script>
                // Función para manejar errores de imagen
                function handleImageError(img) {
                    // Primera alternativa: imagen placeholder local
                    if (!img.dataset.errorTried) {
                        img.dataset.errorTried = 'true';
                        img.src = '/assets/imagenes/placeholder-image.svg';
                        return;
                    }
                    
                    // Segunda alternativa: crear una imagen con canvas igual al placeholder
                    if (!img.dataset.fallbackTried) {
                        img.dataset.fallbackTried = 'true';
                        createFallbackImage(img);
                        return;
                    }
                }

                function createFallbackImage(img) {
                    const canvas = document.createElement('canvas');
                    const ctx = canvas.getContext('2d');
                    canvas.width = 300;
                    canvas.height = 300;
                    
                    // Fondo gris claro
                    ctx.fillStyle = '#e9ecef';
                    ctx.fillRect(0, 0, 300, 300);
                    
                    // Borde principal
                    ctx.strokeStyle = '#adb5bd';
                    ctx.lineWidth = 3;
                    ctx.strokeRect(20, 20, 260, 260);
                    
                    // Marco interior
                    ctx.fillStyle = '#f8f9fa';
                    ctx.fillRect(20, 20, 260, 260);
                    
                    // Dibujar círculo (sol)
                    ctx.fillStyle = '#adb5bd';
                    ctx.beginPath();
                    ctx.arc(100, 80, 15, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Dibujar montañas principales
                    ctx.fillStyle = '#adb5bd';
                    ctx.beginPath();
                    ctx.moveTo(50, 200);
                    ctx.lineTo(120, 120);
                    ctx.lineTo(180, 160);
                    ctx.lineTo(250, 100);
                    ctx.lineTo(250, 280);
                    ctx.lineTo(50, 280);
                    ctx.closePath();
                    ctx.fill();
                    
                    // Montañas secundarias (con transparencia)
                    ctx.fillStyle = 'rgba(173, 181, 189, 0.7)';
                    ctx.beginPath();
                    ctx.moveTo(50, 200);
                    ctx.lineTo(100, 150);
                    ctx.lineTo(150, 180);
                    ctx.lineTo(200, 140);
                    ctx.lineTo(200, 280);
                    ctx.lineTo(50, 280);
                    ctx.closePath();
                    ctx.fill();
                    
                    // Convertir canvas a data URL y asignar a la imagen
                    img.src = canvas.toDataURL();
                }
            </script>
            <script src="/JS/user.js"></script>
            <script src="/JS/carrito.js"></script>
            <script src="/JS/animations.js"></script>
            <script src="/JS/imgError.js"></script>
</body>

</html>
